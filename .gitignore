# cognitive_synergy/.gitignore
# Specifies intentionally untracked files that Git should ignore.
# Lines starting with '#' are comments.

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Environments (Common names for virtual environments)
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE / Editor specific files & directories
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
*.DS_Store
*.swp
*~
nbproject/ # NetBeans project files

# Data files (often large and shouldn't be in Git)
# Adjust this section based on your actual data storage strategy
# It's generally recommended to store large datasets outside the Git repo.
# data/ # Uncomment if your 'data' directory contains large raw datasets
*.csv
*.json # Be careful, might want to keep small config JSONs (like manifests if small)
*.jsonl
*.parquet
*.hdf5
*.pkl
*.joblib
*.pt # PyTorch model files (checkpoints) - handled below too
*.pth
*.pth.tar
*.onnx
*.wav
*.mp3
*.jpg
*.jpeg
*.png
*.gif
*.mp4
*.avi

# Checkpoints and saved models (usually large)
checkpoints/
saved_models/
*.ckpt
# *.pt # Covered above
# *.pth # Covered above
# *.pth.tar # Covered above
# *.onnx # Covered above

# Logs
logs/
*.log
*.log.*
# Log files generated by specific scripts
eval_log_*.log
config_test_log.log
test_log.log

# Experiment tracking artifacts (local only)
# WandB local files (if not using cloud sync or want to ignore local cache/artifacts)
wandb/
# MLflow local files
mlruns/

# Temporary files
*.tmp
*.temp
*.bak

# OS generated files
.DS_Store # macOS
Thumbs.db # Windows

# Jupyter Notebook checkpoints
.ipynb_checkpoints

# Specific project outputs (adjust as needed)
output/
results/
temp_config_test_dir/ # From misc.py example
effective_config.yaml # Saved by train.py

# Secrets / Sensitive Info (should ideally not be in repo at all)
*.key
*.pem
credentials.*
secrets.*
*.env # Often contains secrets

# File Organizer Script (if kept in project root)
# file_organizer.py # Uncomment if you don't want to track this utility


